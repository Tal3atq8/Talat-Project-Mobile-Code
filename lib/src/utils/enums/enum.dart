enum permissionType { camera, medialibrary, photo, storage }

enum SelectLanguage { en, ar }

enum CmsType { aboutus, privacypolicy, termsandcondition, refundpolicy }

typedef NotificationOViewCallback = void Function(bool status);
typedef AlertWidgetButtonActionCallback = void Function(int index);
